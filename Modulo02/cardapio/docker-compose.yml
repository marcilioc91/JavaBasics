services:
  db.cardapio:
    image: mysql:9
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
      MYSQL_DATABASE: cardapio
    ports:
      - '3306:3306'
    volumes:
      - /var/lib/mysql
volumes:
  mysql.cardapio: